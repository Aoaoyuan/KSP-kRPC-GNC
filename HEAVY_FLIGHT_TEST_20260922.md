# 三芯重型火箭第 17 次实飞

## 已验证结果

- 左右侧芯各识别到 7 台可节流主发动机和 4 枚 Sepratron I。侧芯分离后没有再次碰撞中央芯。
- 载荷交接时远地点约 76.1 km，仍以约 400 m/s 向上飞，玩家可以继续顺行加速入轨。
- 两枚侧芯均完成末段软着陆：左侧约 1.9 m/s，右侧约 2.0 m/s。着陆前部件和发动机均保持完整。
- 左右侧芯的最后目标距离分别约 2660 m 和 834 m。第 18 次起，500 m 以下的每条日志都会记录经纬度，用于分别校准两个返场增益。
- 中央芯在 45 km 开始再入点火，速度降到约 1050 m/s 后关机；着陆点火约在 2.65 km 开始。最终以约 2.0 m/s 溅落，剩余约 0.7% 燃料。
- 中央芯分离时有 27 个部件，溅落后剩 25 个；确认损失了两片栅格舵，因此本次不算结构完整回收。

## 本次暴露的问题

单舰 `Vessel.physics_range = 2000000` 已成功回读，但两枚侧芯触地后、完成连续 8 秒落稳确认前仍被 KSP 删除。此时 kRPC 报 `No such vessel`。因此该属性不能替代全局物理范围扩展。

游戏原有 FMRS。FMRS 适合回到分离时刻逐个重飞，不能让玩家操控载荷时三枚芯级在同一时间线继续自动下降。已安装 Physics Range Extender 1.21，并将 `GlobalRange` 设为 2000 km；KSP 1.12.5 启动日志确认程序集 1.21.0.0 已加载。下一次实飞需要验证它能否让两枚侧芯完成落稳确认并保留到手动回收。

## 第 18 次前完成的代码修正

- 支腿改用 kRPC 标准 `Leg.deployed` 布尔接口，不再解析中文 `stateDisplayString`，也不再使用可能反向的 Toggle。
- 300 m 以下由发动机完成末段控制并收回栅格舵，降低触地或入水时折断的风险。
- 完整性验收改为要求部件数保持 100%；不再允许损失 20% 部件后被误判为成功。
- 500 m 以下持续记录实际经纬度，即使载具在落稳确认前被游戏删除，也能取得落点校准数据。
- 全部 43 项离线测试通过。

## 当前状态

- 发射前备份：`codex-heavy-prelaunch-20260922-013324-975845`
- 重启前已确认：`VesselSituation.pre_launch`、当前分级 5、整箭 201 个部件、33 个发动机模块、16 条支腿。
- KSP 已因安装 PRE 重启，目前停在主菜单；需载入上述备份并启动 kRPC 后开始第 18 次实飞。

## 第 18 次与 PRE 安全事故

- 第 18 次首次验证两枚侧芯能在玩家切换到远处载荷后完成 8 秒落稳验收；左、右侧芯均保持 32/32 个部件和 11/11 台发动机。
- 左侧落点越过目标约 2.1 km，右侧越过约 1.0 km。下一版分别把返场速度倍率由 1.20 调到 1.00 和 1.08。
- 中央芯以约 0.8–1.8 m/s 触水，但 300 m 才发送收回栅格舵指令，动画未完成并损失 3 片，因此严格判为失败。下一版在 1800 m 收舵，并把再入关机速度从 1050 m/s 提到 1100 m/s。
- 修正返场自由落体时间公式：下降速度为正的 `v_down` 应取
  `(-v_down + sqrt(v_down² + 2gh)) / g`。旧式加号根会在下降越快时预测出越长的剩余时间，导致返推过量。
- 原版 PRE 1.21 的 `ApplyRangesToVessels()` 会把 2000 km 范围应用到存档中的全部载具。它把 Kerbin 空间站强制载入物理模拟并造成掉轨，同时加载大量旧残骸导致严重卡顿。
- 已强制关闭游戏、禁用原版 PRE，并把主存档恢复为 `codex-heavy-prelaunch-20260922-022236-281465`。恢复后核对：Kerbin 站 283×290 km、Mun 站 157×176 km、Duna 站 107×651 km，均为未加载的稳定轨道。
- 已在工程的 `.reference/PhysicsRangeExtender` 中编译白名单修正版：只有含 `booster_left`、`booster_right`、`booster_core` 或 `payload` 部件标签的载具可获得扩展范围。安装前必须在复制存档中验证，不能直接拿主存档试错。

## 第 35 次完整三芯验证

- 载荷交接保持正东：远地点 75.4 km、垂直速度 333 m/s、水平速度
  1291 m/s、飞行路径角 14.5°、航向 89.0°；上面级未点火，由玩家接管。
- 左侧芯落在目标 35 m 内，右侧芯落在目标 405 m 内。两枚均保持
  32/32 个部件、11/11 台发动机，并连续直立落稳 8 秒。
- 右侧芯此前在最后几十米反向摆动。本次在低于 120 m 且横速降到
  3 m/s 后锁定当地竖直，不再为追求数学上的零横速反向倾斜；实飞通过。
- 中央芯的第一次支腿命令仍因高动压回读为 0/4。控制器按 1 秒间隔
  幂等重发，约 877 m 高度成功确认 4/4；随后以约 2.2 m/s 垂速、
  3.3 m/s 横速在大陆接地，27/27 个部件和 7/7 台发动机完整，连续直立
  落稳 8 秒，剩余约 2.8% 燃料。
- 测试中只在中央芯高空纯滑行段人工使用 2×；进入再入点火前控制器自动
  恢复 1×。正式脚本仍不会主动加速。
- 53 项离线测试全部通过。测试结束后游戏已恢复 1×并暂停。

## 第 36 次重复验证

- 使用与第 35 次相同的代码和飞行参数完成一次独立重复实飞，没有重新调参。
- 载荷交接保持正东：远地点 75.0 km、垂直速度 330 m/s、水平速度
  1288 m/s、飞行路径角 14.4°、航向 90.0°；上面级仍未点火，由玩家接管。
- 右侧芯落在目标 29 m 内，左侧芯落在目标 89 m 内。两枚均保持
  32/32 个部件、11/11 台发动机，并连续直立落稳 8 秒；两个落点都在跑道范围。
- 中央芯的第一次支腿命令回读为 0/4，控制器自动重发后成功确认 4/4。
  它在大陆以约 2.9 m/s 垂速、0.7 m/s 横速接地，保持 27/27 个部件和
  7/7 台发动机，连续直立落稳 8 秒，剩余约 3.2% 燃料。实际落点距规划点
  约 13.7 km，因此属于完整陆地回收，还不属于中央芯精确定点回收。
- 测试中在中央芯高空纯滑行段人工设置 2×；控制器在约 45 km 高度、再入
  点火前自动恢复 1×。正式脚本没有主动设置加速。
- 第 36 次结束后确认物理加速和轨道加速均为 0，游戏已暂停。

第 35、36 次连续两次通过严格完整性验收，当前参数可作为三芯回收的可复现
成功基线。后续若继续提高精度，应单独优化中央芯大陆目标误差，不要破坏已经
稳定的侧芯跑道回收和载荷交接条件。
