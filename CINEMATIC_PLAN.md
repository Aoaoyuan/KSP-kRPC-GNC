# 三芯重型任务摄影与剪辑方案

## 能自动完成的部分

`MissionCamera.py` 通过 kRPC 读取四个控制部件所属的载具，按真实事件而不是
固定时间切镜。它可以自动切换活动载具，选择 Free、Locked 或 Orbital 相机，
并设置距离、俯仰、方位和视场角。每次切镜和关键事件都会写入
`cinematic-cues-日期-时间.csv`，其中同时包含录制秒数和游戏 UT，剪辑时可以
直接定位侧芯分离、中央芯分离和三次着陆。

摄影线程不写油门、姿态、SAS、分级、动作组或发动机状态。飞行与回收仍全部
由 `MyHeavyLaunch.py` 和 `MyRecoverV2.py` 完成。

Stock kRPC 不能隐藏 HUD，也不能把相机固定在任意地面坐标。脚本会提示在开拍
前按 F2；地面机位用远距离、小视场角和低俯仰模拟。当前没有安装 CameraTools，
所以双助推着陆镜头会以较低的一枚助推器为相机主体缓慢跟随，并把另一枚保留
在画面内。它看起来接近跑道旁长焦机位，但不是绝对静止的世界坐标相机。

## 推荐录制方式

正式成片建议录两遍。中央芯分离和双助推末段在时间上接近，KSP 同一时刻只有
一台相机，一遍录制不可能同时保留两个完整动作。

1. 游戏回到三芯火箭发射前存档，保持 1×，打开录屏。
2. 双击 `StartCinematicMaster.cmd`。这遍优先拍中央芯推送、从载荷回望中央芯
   分离、中央芯再入着陆和载荷结尾。
3. 任务结束会自动回到 1× 并暂停。停止录屏。
4. 重新读取同一个发射前存档，再次打开录屏。
5. 双击 `StartCinematicBoosters.cmd`。这遍优先完整拍侧芯分离、翻转返场、
   同步着陆点火、双箭落地和落地后的安静停留。
6. `StartCinematicFlight.cmd` 是一遍式预览配置。它能拍完整任务，但在同时发生
   的事件中优先保留双助推着陆，因此不适合作为唯一的正式素材。

脚本先自动拍六秒低机位整箭，再拍四秒箭底发动机特写，然后才开始三秒发射
倒计时。正常摄影模式不会主动时间加速，也不会在载荷分离时为了飞控强制切回
中央芯。结束时保留九秒载荷与 Kerbin 镜头并暂停游戏。

## 2 分 30 秒建议成片结构

| 成片时间 | 建议长度 | 画面 | 声音与剪辑 |
| --- | ---: | --- | --- |
| 00:00–00:08 | 8 s | 低机位静态整箭 | 环境声，极少文字 |
| 00:08–00:13 | 5 s | 箭底与 21 台发动机 | 点火前机械声，直接硬切 |
| 00:13–00:25 | 12 s | 点火、建立推力、固定广角离塔 | 保留完整离塔动作 |
| 00:25–00:39 | 14 s | 后下方尾焰和重力转弯 | 两个长镜头，避免快速蒙太奇 |
| 00:39–00:49 | 10 s | 远景与 Kerbin 曲率 | 音乐第一次抬升 |
| 00:49–01:05 | 16 s | 三芯分离英雄镜头与远景 | 全片第一高潮，不切掉初始分离 |
| 01:05–01:18 | 13 s | 助推器翻转和同步返场点火 | 使用 boosters 这一遍素材 |
| 01:18–01:30 | 12 s | 中央芯继续推送载荷 | 切回 master 素材 |
| 01:30–01:48 | 18 s | 双助推器共同进入画面并同步着陆 | 第二高潮，一个连续镜头 |
| 01:48–01:53 | 5 s | 两箭落地后安静停留 | 降低音乐，只留环境声 |
| 01:53–02:02 | 9 s | 从载荷方向回望中央芯分离 | 使用 master 素材 |
| 02:02–02:17 | 15 s | 中央芯再入与短再入点火 | 一个远景接一个长焦 |
| 02:17–02:30 | 13 s | 中央芯末段点火和落地 | 保留接地前后动作 |
| 02:30–02:40 | 10 s | 载荷在 Kerbin 前滑行 | 音乐收束，缓慢淡出 |
| 02:40–02:45 | 5 s | 黑屏任务标题 | 仅任务名、日期和“MISSION COMPLETE” |

建议标题：

```text
TRIPLE RECOVERY TEST FLIGHT
KERBIN · [拍摄日期]
MISSION COMPLETE
```

## 画面设置建议

- 录制分辨率优先 1440p 或 4K，帧率 60 fps；成片可输出 24 或 30 fps。
- 关闭鼠标指针、HUD、任务提示和调试窗口；保留发动机、气动和环境声音。
- 不在录制过程中使用时间加速。长滑行段在后期压缩或直接跳切。
- 镜头间以硬切为主；开头、载荷结尾和黑屏可使用短淡入淡出。
- 颜色以自然对比为主，避免高饱和游戏滤镜。高空镜头可以略降曝光以保留
  Kerbin 边缘和发动机尾焰层次。
- 双助推着陆不要拆成左右两个近景。同步感来自同一画面内的共同点火和下降。

## 可选升级

若以后安装 CameraTools，可以把开场、离塔和双助推着陆升级为真正固定在世界
坐标的地面相机，并使用路径相机完成平滑摇摄。CameraTools 不属于当前依赖，
现有脚本即使没有它也能完成任务，且不会因摄影功能失败而中止飞控。
