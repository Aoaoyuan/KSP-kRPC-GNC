# 发射与回收联动

入口为 `MyLaunchV2.py`，会直接调用 `MyRecoverV2.py`。原版 `MyLaunch.py` 和 `MyRecover.py` 保留。

## 先配置火箭

- 在助推器上保留的正向控制舱/探测核心设置唯一 kRPC 部件标签 `booster`，其控制方向应朝向火箭头部。它既用于识别助推器，也用于分离后设置“从这里控制”。
- 在载荷上保留的部件设置唯一标签 `payload`。标签是 kRPC 部件标签，不是载具名称或部件标题；可通过游戏内 kRPC 部件标签界面设置。
- 助推器必须保留控制能力、电量、燃料和可重启的着陆发动机。
- 本版分级顺序与旧脚本一致：执行一次起飞分级，再执行一次载荷分离分级。两者必须相邻，不支持中途额外分级。分离级不要混入载荷发动机点火动作；载荷后续入轨需要独立方案。

## 运行

先在发射台只读检查：

```powershell
python MyLaunchV2.py
```

例如游戏分离动作位于 **第 1 级**，执行：

```powershell
python MyLaunchV2.py --execute --separation-stage 1 --leg-offset 16
```

这里的 `1` 仅为示例，必须换成火箭实际分离级编号。当前级数应为 3，起飞激活第 2 级，分离激活第 1 级。默认检查不点火、不分级；执行前自动另存 `codex-before-launch-时间戳`，保存失败则不发射。

若动作组 8 用于栅格翼展开，可追加 `--grid-group 8`，只在回收开始时设置开启。支腿参数的校准方法见 `RECOVERY_V2.md`。

需要 `krpc` 包；若用本工程的 `.test-python-deps` 依赖目录，可先设置 `$env:PYTHONPATH = (Join-Path $PWD '.test-python-deps')`，并使用可用的 Python 解释器运行。

## 如何解决分离后选中载荷

1. 发射前记录两个标记部件的对象引用。
2. 分离前关油门，退出旧自动驾驶并移除旧遥测流。
3. 只触发一次分离，通过两个部件各自的 `part.vessel` 判断它们是否真正成为不同载具。
4. 把 `space_center.active_vessel` 设为助推器，设置助推器控制舱为控制基准。
5. 将明确的助推器对象传给回收入口，重新创建它的全部遥测流。

不根据名字、体积、返回列表顺序或旧活动载具对象判断身份。未确认分离时超时退出，不反复按空格。标记部件必须存活；本版不跨读档复用对象。

## 发射控制和范围

从 250 m 开始指数转弯，30 km 达到 10°俯仰；按质量和推力限制推力加速度约 20 m/s²，接近 75 km 远地点时逐渐降低油门。随后滑行、水平补速并在目标高度附近分离。补速明确使用天体非旋转参考系，默认速度 1580 m/s，不保证与旧脚本特殊参考系的同数值速度等价。保留经验飞行路径，尚不具备定点落区规划或上面级圆化入轨。

加入推力丢失、下降过早、目标超时、分级不符、活动载具改变和时间异常检查。异常会尽量关闭助推器油门并退出自动驾驶，不能保证挽救已经失控的飞行。

离线测试：`python -m unittest -v test_launch_v2 test_recover_v2`。包含载荷被选中、同名载具、部件归属延迟、分离未完成等情况；尚需 KSP 实飞验证。
