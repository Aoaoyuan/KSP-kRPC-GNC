# 本项目的 GNC 控制链路

这里的 **GNC** 分为三层：Guidance 决定“应朝哪里、目标速度是多少”，Navigation
提供位置速度等测量值，Control 把目标转成自动驾驶姿态和发动机油门。

## 1. 发射与分离

`MyLaunchV2.py` 先在当地地表参考系中控制发射姿态。inland 剖面近乎竖直，目的是
让助推器仍可返回发射场；下程发射才使用重力转弯来换取水平速度。远地点构成外环，
`ascent_throttle()` 在接近目标远地点时收油，并限制最大加速度。

分离不依赖载具名称或 KSP 返回列表的顺序。脚本通过 `booster` 和 `payload` 两个部件
标签重新确认部件归属。这样分离后可把活动载具留给载荷，同时由另一个 kRPC 连接控制
助推器。

## 2. 坐标与导航量

`MyRecoverV2.py` 使用 Kerbin 固连、原点位于 Kerbin 中心的参考系。对载具位置向量
`r` 归一化得到当地向上方向：

```text
u = r / |r|
v_vertical = dot(v, u)
v_horizontal = v - v_vertical * u
g = mu / |r|^2
```

这种写法不假设某个世界坐标轴永远是“上”，所以可用于任何经纬度的着陆点。

## 3. 回收状态机

`Guidance.state` 依次经过：

1. `COAST`：油门为零，下降时朝向相对速度反向。
2. `SEPARATION`：分离后先沿远离载荷的相对位置方向取得安全间距。
3. `BOOSTBACK`：对准预测返场方向后，做一次短促的横向 Δv 脉冲并立即关机。
4. `REENTRY`：高速进入稠密大气前，短暂减热/减速。
5. `BURN`：达到制动高度后满油门，压缩重力损失。
6. `TERMINAL`：跟踪随高度收紧的下降速度包线，并做横向误差修正。

这三次动力段对应猎鹰 9 的回收思路：返场点火只改变下程速度，绝不承担悬停；再入点火
用于控制气动载荷；着陆点火尽可能晚开始，以较小的重力损失消除剩余垂直速度。项目中的
具体发动机数量、点火门限和目标位置仍由这枚 KSP 火箭的推重比、阻力和结构限制决定。

动力制动的触发距离由下式估算：

```text
s_brake = v_down^2 / (2 * a_net) * margin
          + v_down * reaction_time + terrain_margin
a_net = (T / m) * cos(max_tilt) - g
```

其中 `T / m` 是最大推力加速度。公式只给理想竖直制动距离，因此代码加入姿态、发动机
和地形裕量。实际飞行中 `BURN` 满油门，直到速度已能被末端包线容纳，才切换到
`TERMINAL` 的连续油门控制。

`SEPARATION` 是返推的硬性前置条件。它以载荷到助推器的相对位置为推力方向，并以
25 m/s 为目标相对速度。助推器至少等待 20 秒且与载荷相距 100 m 后才允许
`BOOSTBACK`，避免分离后返推轨迹与载荷再次相交，同时保留高空返推窗口。

返场点火的速度误差使用迟滞：误差达到 4 m/s 才开始，降到 1.5 m/s 即关机。这避免临界
误差附近不断点火；自动驾驶对准返场方向前油门保持为零，因此转向本身不会产生无意义的
Δv 消耗。

为减少发动机开机次数，`SEPARATION` 不点主发动机；`BOOSTBACK` 一旦点火即锁定方向、
保持连续油门至误差进入死区，然后明确切到 `COAST`。再入和着陆同样各自是一段连续的
点火窗口。支腿在预计触地约 8 秒时逐条发送展开命令，并回读模块状态；触地后还会比较
分离时与着陆时的部件、发动机数量，残骸不会误报为成功回收。

预测的自由落体时间没有包含阻力、姿态转向和末段倾角限制。`boostback_return_gain` 因此是
一个可标定系数；默认值为 1.0。只有部件和发动机数量均通过完整性验收的着陆，才能用于
更新它。残骸落地没有导航标定价值，不能把一次炸毁的位置当成普适的落点误差。

## 4. 末端速度与横向控制

末端目标速度使用：

```text
v_target = -sqrt(v_touchdown^2 + 2 * a_net * 0.35 * height)
```

它相当于把剩余高度的一部分用作制动距离，保证目标速度随高度平滑降低。PI 控制器把
垂直速度误差转成所需向上加速度；横向位置误差先转成有限的水平速度目标，再受最大
倾角限制，防止临地为追点而侧翻。

## 5. kRPC 的后台控制与时间加速

读档后或后台控制非活动助推器时，KSP 可能接受总油门属性却不实际施加推力。
`FlightControl.ThrottleController` 在后台模式强制使用各发动机的 `independent_throttle`。

`--allow-warp` 不会自动加速游戏，玩家可在高空滑行手动加速。代码根据当前制动距离再
预测 30 秒自由下落距离，在进入该区域前恢复 1×。不带该参数时，检测到加速同样会退回
1×，但脚本继续控制而不退出。

## 6. 目前的范围

默认目标已设置为本存档发射台的实测坐标。当前横向闭环在动力回收阶段工作，适合先做
KSC 附近的定点着陆标定。后续要提高远距离返场精度，需要在 `COAST` 阶段加入落点预测，
并把多个 `booster-*` 标签交给独立回收任务管理器。

屋顶停机坪不能只用地形高度处理：`surface_altitude` 是相对地形的读数，而楼顶本身高于
地形。将标记载具停在停机坪中心后运行 `CalibrateLandingSite.py`，把输出的经纬度和
`--landing-surface-offset` 传给发射脚本。回收器会用“地形高度 + 屋顶高度 + 支腿质心
高度”计算真正剩余制动高度。
