# 回收脚本 V2

`MyRecoverV2.py` 是助推器再入与动力着陆控制器，不包含返航、落点预测或定点导航。原有两个脚本未改动。

## 开始使用

1. 在自己的 Python 环境中安装 `krpc`：`python -m pip install krpc`。
2. 启动游戏内 kRPC 服务（本机 RPC 50000、Stream 50001），关闭时间加速。
3. 切换到已经分离的助推器；确认着陆用发动机已激活、燃料充足、可重启且支持连续节流。脚本不会分级或自动选择发动机。
4. 运行 `python MyRecoverV2.py`，只检查当前载具和连接。
5. 运行 `python MyRecoverV2.py --execute --vessel "助推器名称" --leg-offset 16` 开始接管。名称检查是精确匹配。

如动作组 8 配置为展开栅格翼，可附加 `--grid-group 8`。默认不改变动作组；脚本设置组状态为 True，不盲目 toggle。若动作绑定本身是切换动作，仍应在游戏中确认初始状态及绑定。

`--leg-offset` 是支腿展开并平稳落地时的质心对地高度，可在地面用只读检查测量。默认 16 m 来自旧脚本，未经校准。它只用于估计支腿离地高度，不是关机高度。脚本以游戏 landed/splashed 状态确认接触，接触时立即关油门，状态持续 0.5 游戏秒后结束。

Ctrl+C 会尝试关油门、退出自动驾驶、关闭 RCS。异常同样处理，断线时无法保证命令送达，需要检查游戏。正常完成后保留起落架和动作组状态，不恢复先前 SAS/RCS 设置。

## 控制方式

- 使用天体地固系的速度与位置，按径向投影求垂直和水平速度；自动驾驶目标也在同一坐标系。
- 高空下降时指向对地逆速度；海拔低于 26 km、速度超过 1200 m/s 时使用 33% 油门，降到 1050 m/s 后关机。此再入策略沿用旧脚本经验值，并非热防护规划。
- 按当前推力、质量和下降速度估算制动高度，提前进入着陆控制；进入后不返回高空阶段。
- 用随高度下降的目标速度控制着陆。重力前馈提供基本悬停推力，速度 PI 修正下降速度，并用实际倾角补偿竖直推力。
- 对水平速度施加反向倾斜制动，最大 20°，支腿离地 15 m 以下最大 5°。临地目标下降速度趋近 0.8 m/s。
- 姿态严重偏离时暂不点火；这不能保证挽救已经翻转且高度不足的载具。
- 统一使用游戏时间，遥测以 20 Hz 请求更新；处理低推力、时间回退、时间加速、活动载具改变和超时。

## 调参和验证范围

主要参数集中在 `Config`。先校准支腿高度，再根据试飞调整下降速度、最大倾角和速度控制增益。假设发动机合推力基本沿箭体朝向、油门和推力近似线性，不能直接适配不可节流发动机或严重偏置的发动机布局。脚本使用 kRPC 自动驾驶的默认调节，柔性大箭可能需要单独调姿态参数。

水平制动不含目标位置反馈；高横向速度、山坡、地形突变、燃料不足和推力延迟可能导致失败。建议先在快速存档上试飞。

离线验证：`python -m unittest -v test_recover_v2`。
已通过 8 项数学测试，其中点质量模拟覆盖 1500 m/-100 m/s、4000 m/-250 m/s、100 m/0 m/s 三组初始条件；模拟无阻力、无姿态或推力延迟，不代表已完成游戏实飞验证。
