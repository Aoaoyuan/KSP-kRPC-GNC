# B1040.7 联动实飞记录

日期：2026-09-19；游戏 kRPC 服务版本：0.6.0。

结果：完成发射、载荷分离、自动切回助推器、再入制动、末段减速和海面溅落。未实现陆地或定点回收。

## 配置与修正

- 火箭初始质量约 361707 kg，7 台矢量发动机，当前分级 2；第 1 级起飞、第 0 级载荷分离。
- 助推器 OKTO2 标记为 `booster`，载荷 Mk1-3 指令舱标记为 `payload`；实测探测核心朝向与箭体一致。
- 使用 `--separation-stage 0 --leg-offset 16 --grid-group 8`。通过栅格翼事件变为 `Retract Fins` 确认四片均已展开。
- 首次未点火即因旧自动驾驶方法缺失退出。已将发射和回收的 `engage()/disengage()` 改为本机 0.6.0 的 `engaged` 属性。
- 第二次运行完成完整飞行；远地点约 75 km，分离后活动载具为 `B1040.7 探测器`，控制基准为 OKTO2。

## 落地结果和限制

- 游戏明确报告 `VesselSituation.splashed`；关机、自动驾驶退出、RCS 关闭均已核实。
- 最后几条触水前控制日志垂直速度约 -0.6 m/s，水平速度约 0.2 m/s。此为日志采样值，不是碰撞瞬间测量。
- 溅落后仍有 27 个部件；最终液体燃料约 47.88 单位。
- 落点约南纬 0.07823°、西经 41.47658°，位于海面。
- `16 m` 支腿质心偏移未完成陆地校准，海面接触高度与支腿陆地接触不同。
- 末段低速下降耗油较多，燃料余量很小；不应将本次单次成功解释为可靠性验证。
- 下一步优先考虑落点预测/海陆判断、燃料预算以及减少高空长时间低速下降；尚未实现这些功能。

## 可恢复存档

- 原始未标记火箭：`codex-original-20260919-004948`
- 第二次发射前：`codex-before-launch-20260919-005029-044142`
- 分离后：`codex-test02-after-separation`
- 溅落后：`codex-test02-splashdown`

本次结束后留在溅落状态，没有自动读档或开始新一轮发射。

原始输出见 `flight-test-01.log`、`flight-test-02.log`；最终遥测见 `flight-test-02-result.json`。
