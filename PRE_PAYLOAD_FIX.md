# PRE 载荷震动：修复进展与安装

## 当前结论（2026-09-24）

用户实测确认：第一版仅缩小活动载荷物理范围的补丁没有解决零件间扭动。
第二版也未解决用户实测的问题。第三版修正了已查实的任务身份识别错误，已编译，尚未安装和实飞验收。
Python 测试通过不代表 Unity 物理行为已验证。

现场只读遥测：载荷约在 82 km 轨道上，物理范围 2500 m，SAS、RCS、自动驾驶均关闭，
油门与三轴控制输入均为零；一枚已落地侧芯仍为 loaded=True（当时同时 packed=True）。
这说明第一版范围设置确实生效了，残留控制并非本次采样中的原因。

检查本机 KSP 程序集发现：Krakensbane.SafeToEngage 遍历全部已加载载具，
只要另一个载具已落地/溅落，或雷达高度低于 200 m，就拒绝速度参考系修正，
同时通过 FloatingOrigin.SetSafeToEngage 阻止自动原点平移。这里不排除 packed 载具。
所以“已打包”和“已退出加载列表”不是同一回事；远距加载的落地芯级仍可能阻塞精度保护。
载荷继续高速飞行时，这与用户报告的结构扭动一致，但仍需要前后对照确认因果。

另一个可疑点是 PRE 把扩展距离的平方写入 FloatingOrigin.threshold。
本机 KSP 实际使用 Awake 时缓存的 thresholdSqr，因此不能仅凭这行代码就断言
它是本次震动的直接根因。候选补丁删除这次不必要的写入，不改游戏的精度保护阈值。

## 第三版：修正此前遗漏的身份识别错误

再次现场检查发现：当前载荷有 kRPC 的 payload 标签，已落地侧芯却没有任何 kRPC 标签。
进一步核对本机 KRPC.SpaceCenter.dll：Part.Tag 的读写实际访问
KOSNameTag 模块的公开 nameTag 字段，而不是 Unity Component.tag。
前两版 PRE 白名单错误地使用 part.tag，因此落稳检查和精度日志可能根本没有识别到任务载具。
第二次现场对照尚未开始时，用户已自行通过开关 PRE 缓解震动；不能把这一效果归功于补丁。

第三版改动：

- 通过 KOSNameTag.nameTag 读取与 kRPC 一致的标签，不再读取 Unity 的 part.tag。
- 允许回收线程已经写入存档的唯一名称作为侧芯/中央芯备用身份：
  Codex booster_left/right/core 加一个空格和十位小写十六进制 ID。
  必须完整匹配；带残骸后缀、任意前缀、其他角色或错误长度均不匹配。
- 当前载荷的默认范围每秒刷新，覆盖标签在载具创建事件之后才写入的情况。
- 场景启动日志输出 build=20260924-identity-v3，即使 PRE 关闭也输出版本及启用状态。
  启用 PRE 后应看到每十秒一次的载荷精度日志。
- tools/TestPreIdentity.ps1 直接提取候选源码中的识别方法，用小型接口替身编译验证。
  16 项检查覆盖正确字段、丢失标签、空部件列表、三个芯级角色、残骸和无关空间站。
  测试只验证身份识别，不代替 Unity 物理实测。

本机参考源码存在时运行：pwsh -File tools/TestPreIdentity.ps1。
其他环境可用 -SourcePath 指向已应用补丁的 PhysicsRangeExtender.cs。
源码补丁可在声明的上游版本上完整应用，生成内容与编译源码一致。

## 落地范围策略（由第二版引入，第三版修正其身份识别）

- 保留活动载荷默认范围、飞行芯级及后台载荷的远距范围；无关空间站不扩展范围。
- 每秒检查带任务标签的已加载载具。连续落地/溅落 8 秒，且未打包时地速不超过
  0.5 m/s、角速度不超过 0.05 rad/s，才视为可以停止远距物理模拟。
- 只有玩家正在操纵同一天体上的高空载具（高于大气层，且至少 10 km）、该落地芯级
  距活动载具超过 10 km 时，才给落地芯级恢复游戏默认加载范围，让 KSP 正常卸载。
- 落地载具仍保存在存档和追踪站中，不调用 Recover，不删除载具，不切换到 KSC。
  切回该芯级时，原有切换逻辑重新处理范围；若芯级再次起飞或恢复明显运动，取消落稳标记。
- 不卸载仍在飞行、下降或尚未落稳的芯级。不通过强行移动载具、关闭碰撞等方式掩盖震动。
- 修复 PRE 在关闭状态进入场景后缺少事件订阅，以及退出场景时错误重复订阅的问题。
- 每 10 秒输出一次 [PRE Precision] 日志：活动载具离世界原点的距离、参考系速度、
  仍阻塞精度保护的已加载载具；用来核对实际效果。

“保留助推器直到手动回收”应指保存落地载具，而非永久保持数千公里物理模拟。
后者会与 KSP 的精度保护产生冲突。修改 Python 脚本本身无法替换已加载的 PRE DLL。

## 编译与安装

补丁文件：patches/PhysicsRangeExtender-managed-payload.patch。
基于 jrodrigv/PhysicsRangeExtender 提交
4b726aeba4bb202dcd4b9776c62ad0e6ca678ad1，包含完整白名单和两版范围修复。

在该源码仓库根目录应用补丁，将 C# 项目引用配置到本机 KSP Managed 程序集，编译 Release。
完全退出 KSP 后，备份并替换 GameData/PhysicsRangeExtender/Plugins/PhysicsRangeExtender.dll。
重启才会使用新 DLL；不要在游戏仍运行时覆盖。保留旧 DLL 即可回滚。
本仓库提供源码补丁，不分发游戏程序集。

## 待验收与限制

应使用存档副本验证：

1. 载荷在轨道持续 1×运行；远处已落稳侧芯退出 loaded 列表，但仍存在于存档与追踪站。
2. 精度日志中的落地阻塞项消失，载荷零件不再持续扭动，轨道与部件数无异常变化。
3. 仍在下降的中央芯和侧芯继续受控，未被提前卸载。
4. 切回已落地芯级时，结构完整、位置正确；切回载荷后仍能正常退出远距加载。
5. 开关 PRE、读档及反复切换场景后，事件和范围策略仍正确。

若仍有另一枚芯级在 200 m 以下下降，KSP 的原生保护条件仍会阻塞参考系修正。
该候选补丁不强行绕过这一保护，也不能保证所有同时异地飞行阶段都无震动。
还可能存在自动加固、柔性连接或其他模组的结构问题，应结合日志继续排查。
